<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheet/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class=" bg-zinc-900 h-full w-full text-white p-10  ">
    <div class="justify-end w-full flex">
      <a class="bg-red-500 px-3 py-1 rounded mb-2 inline-block " href="/logout">logout </a>
    </div>
    <h1 class="text-3xl mb-3 ">Profile <%= user.name %>
    </h1>
    <h3 class="mb-3">You can create a new post </h3>
    <form action="/post" method="post">
      <textarea class=" outline-none border-2 p-2  resize-none block border-zinc-500 bg-transparent rounded w-1/3 "
        name="content" placeholder="whats on your mind ?"></textarea>
      <input type="submit" class="bg-orange-600 px-3 py-1 rounded w-40 mt-2 block" value="Create Post">
    </form>
    <div class="posts mt-5  ">
      <h3 class="text-zinc-400 ">Your posts</h3>
      <div class="postcontainer mt-2 flex flex-wrap gap-3">
        <% user.post.reverse().forEach(function(post){ %>
          <div class="post w-1/3 p-5 mb-2 border-2 border-zinc-700 bg-zinc-800 rounded">
            <h4 class="text-blue-500 mb-2">
              <%= user.username%>
            </h4>
            <p class="tracking-tight">
              <%= post.content %>
            </p>
            <small class="inline-block mt-3">
              <%= post.likes.length %> like
            </small>
            <div class="btns flex gap-5">
              <a class="text-blue-500 mt-3" href="/like/<%= post._id%>"><%= post.likes.indexOf(user._id)===-1 ?"Like":"Unlike" %></a>
              <a class="text-red-600 mt-3" href="/edit/<%= post._id %>">Edit</a>
            </div>
          </div>
          <% }) %>
      </div>

    </div>
  </div>
</body>

</html>